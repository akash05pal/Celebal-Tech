SELECT W.ID, P.AGE, W.COINS_NEEDED, W.POWER FROM WANDS W JOIN WANDS_PROPERTY P ON W.CODE = P.CODE WHERE P.IS_EVIL = 0 AND W.COINS_NEEDED = (SELECT MIN(W2.COINS_NEEDED) FROM WANDS W2 WHERE W2.POWER = W.POWER AND W2.CODE = W.CODE) ORDER BY W.POWER DESC, P.AGE DESC; 